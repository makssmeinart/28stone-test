import { PageHolder } from 'src/main/components';
import { GlobalStyles } from 'src/main/styles/globalStyles';

export const App = () => {

  return (
    <>
      {/* Global Styles + Color pellet */}
      <GlobalStyles />
      <PageHolder />
    </>
  )
}